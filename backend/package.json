{
  "name": "backend",
  "version": "1.0.0",
  "description": "Simples backend em JSON",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Raphael Cunha",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
